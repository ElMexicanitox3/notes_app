import 'package:flutter/material.dart';

class ConfigLang extends StatelessWidget {
  const ConfigLang({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Language"),
      ),
    );
  }
}